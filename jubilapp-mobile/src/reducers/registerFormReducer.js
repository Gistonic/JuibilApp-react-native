const INITIAL_STATE ={
    email: "",
    name:"",
    surname:"",
    phone:"",
    password:""
}
const registerFormReducer = (state = INITIAL_STATE, action)=>{
    switch (action.type) {
        default: return state

    }

}